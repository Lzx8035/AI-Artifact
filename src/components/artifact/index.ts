// 对外 API 表面:接入方只需从这里导入组件与类型。
export { ArtifactWorkspace } from "@/components/artifact/workspace";
export type { ArtifactWorkspaceProps } from "@/components/artifact/workspace";
export type {
  Artifact,
  HtmlArtifact,
  HtmlFiles,
  ReactArtifact,
  ReactFiles,
} from "@/lib/artifact";
